# Runtime dependencies, plus everything needed to test, lint, and build
# distributable binaries. `pip install -r requirements-dev.txt` is
# everything a contributor needs.
-r requirements.txt

black==26.5.1
click==8.4.2
iniconfig==2.3.0
mypy_extensions==1.1.0
packaging==26.2
pathspec==1.1.1
Pillow==12.2.0
platformdirs==4.10.1
pluggy==1.6.0
Pygments==2.20.0
pyinstaller==6.22.0
pytest==9.1.1
pytokens==0.4.1

# Pillow is only needed to run resources/makeicon.sh (regenerates
# MyIcon.ico/.png from icon.png) -- not imported anywhere in src/.
